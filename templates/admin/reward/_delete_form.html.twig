<form method="post" action="{{ path('app_admin_reward_delete', {'id': reward.id}) }}" onsubmit="return confirm('Supprimer cette récompense ?');" style="display:inline-block">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reward.id) }}">
    <button class="btn btn-sm btn-outline-danger ms-1"><i class="bi bi-trash"></i></button>
</form>
